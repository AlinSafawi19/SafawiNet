{
  "name": "safawinet",
  "version": "1.0.0",
  "description": "Safawinet application with API and infrastructure",
  "private": true,
  "scripts": {
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "docker:restart": "docker compose restart"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "@types/winston": "^2.4.4",
    "winston": "^3.17.0"
  }
}
